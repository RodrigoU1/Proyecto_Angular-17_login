// Una interface es un contrato que especifica los atributos
// y metodos que deben ser implementados por cualquier clase que
// la implemente

export interface LoginRequest {
    email: string,
    password: string,
}